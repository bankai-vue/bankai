{
  // Solution config: `tsconfig.app.json` covers the browser app (vue-tsc),
  // `tsconfig.node.json` covers Node-land tooling like `vite.config.ts`.
  "files": [],
  "references": [{ "path": "./tsconfig.app.json" }, { "path": "./tsconfig.node.json" }]
}

---
'@bankai-vue/theme-tailwind': minor
---

Style `BankaiInput` in the Tailwind theme, `@apply`-first — the field speaks the consumer's Tailwind build and matches the stock utility form-control look. The box is drawn with an **inset `outline` rather than a `border`** (with `border-0` clearing the UA border, since the theme ships without Preflight): a 1px inset outline at rest that `:focus-visible` thickens to 2px and recolors, so there's no separate detached ring and no layout shift. Sizes are tuned to conventional control heights — **sm 32px / md 36px / lg 40px** (border-box, single line): line-height `24px` with `py-1`/`py-1.5`/`py-2`, `px-2.5`/`px-3`/`px-3.5`, and lg stepping the font to `text-base`. Colors pin to the neutral/indigo ramp (like the button here) through a `--bankai-input-*` override surface: gray-300 inset outline + gray-400 placeholder + white field at rest, the accent role on focus, and a raised surface fill for `:read-only` (excluding `:disabled`). `light-dark()` keys the dark treatment off `color-scheme` — a translucent field (`white/5` fill, `white/10` outline, gray-500 placeholder) — with no Tailwind `dark:`. `:disabled` is a filled, dimmed field rather than a transparency drop: a gray fill (`gray-50` light / `white/10` dark), muted `gray-500` text, a fainter outline than at rest (`gray-200` / `white/5`), and `cursor-not-allowed`. Every rule stays wrapped in zero-specificity `:where()`, so a consumer's own CSS/utility classes override without `!important`.

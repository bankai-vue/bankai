---
'@bankai-vue/theme-bankai': minor
---

Style `BankaiButton` — targets the `bankai-button` class and reflected state (`data-bankai-variant`, `data-bankai-size`, native `:disabled`) at zero specificity (`:where()`), with per-component `--bankai-button-*` tokens.

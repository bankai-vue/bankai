---
'@bankai-vue/theme-bankai': minor
---

Style `BankaiButton` — targets the `bankai-button` class and reflected state (`data-variant`, `data-size`, native `:disabled`) at zero specificity (`:where()`), with per-component `--bankai-button-*` tokens.

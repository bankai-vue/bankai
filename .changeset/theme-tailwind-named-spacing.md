---
'@bankai-vue/theme-tailwind': minor
---

Mirror the named t-shirt spacing steps — `--bankai-space-xs`…`--bankai-space-2xl`, pointing at the same numeric steps as the house theme (index-preserving) so a named spacing prop like `BankaiFlex gap="md"` means the same grid position under either theme, sized to Tailwind's grid. Parity with `@bankai-vue/theme-bankai`.

{
  // https://nuxt.com/docs/guide/concepts/typescript
  // Generated by `nuxi prepare` (runs on postinstall); references the per-context
  // configs Nuxt emits under .nuxt/.
  "extends": "./.nuxt/tsconfig.json"
}

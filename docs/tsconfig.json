{
  // https://nuxt.com/docs/guide/concepts/typescript
  // Extends the config Nuxt generates under .nuxt/ — created by any `nuxi` run
  // (dev/build/generate/prepare). Because .nuxt/ is absent until then, the shared
  // root oxlint run excludes docs/ (see .oxlintrc.json); Nuxt's own `nuxi typecheck`
  // owns type-checking here.
  "extends": "./.nuxt/tsconfig.json"
}
